import { Inter, Lora } from 'next/font/google';

export const lora = Lora({
  subsets: ['latin'],
  variable: '--font-lora',
});

export const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
});
